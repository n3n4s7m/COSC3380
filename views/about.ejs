<!DOCTYPE html>
<html lang="en">
    <body>
        <%if(isAdmin == null) {%>
            <%- include('./_header') %>
        <%}
        if(isAdmin == 1) {%>
            <%- include('./_adminheader') %>
        <%}
        if(isAdmin == 0) {%>
            <%- include('./_userheader') %>
        <%}%>
        <div class="container">
            
            <div class="row">
                <div class="col-2">
                    <h1>About </h1>
                    <p>stufffffffffffffff</p>
                </div>
            </div>
        </div>
    
    </body>
</html>